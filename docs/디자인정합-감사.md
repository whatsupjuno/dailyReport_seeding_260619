## 디자인 정합 감사 — 화면별 확정 차이 (3패스 병합·중복제거·콘텐츠 오탐 제거 후)

> 콘텐츠 오탐(이름·날짜·카운트), 라벨 일치 항목, 데모 더미 행 아티팩트(직원 뷰 빈 아바타·좌측 강조선), 테이블 완결율 바 색(디자인 데모 행 공란이라 관측 불가) 등은 모두 제외함. 소스 라인은 현재 파일 기준 재검증 완료.

### 1. write (작성 화면 — 직원 빈작성 / 방준호 반려)

| 영역 | severity | design | impl | fix |
|---|---|---|---|---|
| 2단계 스테퍼 카드 | P1 | 페이지 헤더 다음 곧바로 '오늘 할 일' 카드. 스테퍼 카드 없음 | 항상 흰 카드 안 '작성→제출' 스테퍼 + '오전/오후는 단계가 아니라 완료 시각 분류입니다' 렌더 | `ReportEditor.tsx:488-510` 스테퍼 카드 블록 제거(또는 작성 화면 비노출) |
| 전역 상단 역할 전환 세그먼트 | P1 | 상단바 중앙 '역할 전환 · 데모' 라벨 + 직원/그룹장/관리자 세그먼트(활성 파란 알약) | 로고 + 프로필 알약 + 로그아웃만, 세그먼트 없음 | `AppShell.tsx`(상단바, 34-120 인근)에 데모 역할 세그먼트 추가 — 데모 하네스 성격이라 제품 노출 결정 필요 |
| 페이지 헤더 우측 인디케이터 | P1 | '마감까지 1시간' 주황 알약 + '✓ 저장됨 · 오후 5:52' 자동저장 표기 | 상태 뱃지만 있음, 둘 다 없음(`마감까지`/`저장됨` 문자열 src 전체에 부재) | `ReportEditor.tsx:457-464` 헤더에 마감 알약 + 저장됨·시각 인디케이터 추가(자동저장 시각 상태 필요) |
| 오늘 할 일 카드 우상단 버튼 | P1 | 라벨 '근태 변경' | 라벨 '휴가로 전환' | `ReportEditor.tsx:550` 텍스트 '휴가로 전환'→'근태 변경' |
| 오늘 할 일 헤더 카운트 칩 | P1 | '할 일 N · 진행중 M' 형식 | '남은 N · 완료 M' (`남은 {view.todo.length} · 완료 {doneCount}`) | `ReportEditor.tsx:548` 라벨/계산을 '할 일 N · 진행중 M'(진행중=status 진행중 수)으로 |
| 결과 바구니 카운트 뱃지 | P1 | 상태 분해 '완결 2' / '완결 0 · 지연 1' (초록 톤 강조) | 중립 회색 단순 총건수 `{tasks.length}건` | `ReportEditor.tsx:964` 뱃지를 '완결 X · 지연 Y'(완결만 있으면 '완결 X'), 색 초록(#1F9254 계열)로 |
| 결과 바구니 업무 행 '예정·실제' 메타 | P1 | 완료 행에 '예정 2h · 실제 2h 10m', '예정 30m · 실제 25m' 회색 메타 라인 | 완료 행(978-986)에 예정·실제 라인 없음(todo 행 942엔 예정만, 실제 없음) | `ReportEditor.tsx:978-986` 제목 줄 다음 '예정 … · 실제 {actual}' 추가. 실제 소요(actualDurationMin) 데이터 노출 필요. #9AA1AE/12px/tnum |
| 오후 바구니 시간 범위 | P2 | 오후 '12:00 ~' 열린 형태 | 오후 '12:00 ~ 19:59' 종료시각 명시 | `ReportEditor.tsx:659` range '12:00 ~ 19:59'→'12:00 ~' |
| 예정 소요 시간 단위 | P2 | 'h/m' 약식 ('예정 15:00 · 2h', '30m') | `${plannedDurationMin}분` 분 정수 표기 | `ReportEditor.tsx:942` + 바구니 메타에 분→시/분 약식 포맷터 적용 |
| 좌측 사이드바 아이콘 + 시간대 서브섹션 | P2 | 메뉴 라벨 앞 아이콘(연필/리스트/체크원/사람들) + 하단 '시간대' 구분선 + 서브메뉴(작성/보기(제출본)/반려(재작성)) | 아이콘 없는 텍스트 메뉴만, 구분선·서브메뉴 없음 | `AppShell.tsx`(13-18 NAV, 135-160 nav)에 아이콘 + '시간대' 서브섹션 추가. 서브메뉴는 데모 하네스라 제품 결정 필요 |
| 미착수 상태 칩 라벨 | P2 | 미착수 칩 라벨 '예정' (진행 중은 '진행중' 파란 칩) | 동일 상태를 '계획'으로 표기 (status를 그대로 칩 텍스트로 렌더) | `ReportEditor.tsx:937/982` status==='계획'일 때 '예정' 표시, 또는 status.ts 표시 라벨을 도메인 값과 분리. 칩 색 동일, 텍스트만 변경 |

### 2. list-mgr (팀 보고 현황 — 관리자)

| 영역 | severity | design | impl | fix |
|---|---|---|---|---|
| KPI 요약 통계 카드 4개 | P0 | 타이틀 아래 4개 카드: ①오늘 제출 14/18명(진행률 바 파랑·'진행률 78%') ②미제출 4명('독려가 필요해요') ③지연 3건('마감 초과', 빨강+! 아이콘) ④검수 대기 6건('확인이 필요해요', 주황+모래시계 아이콘) | KPI 카드 행 통째로 없음. 타이틀 바로 아래 필터 폼 | `reports/page.tsx` 타이틀(77)과 폼 사이에 4개 stat 카드 섹션 신규. `all` 배열에서 집계. 진행률 바(파랑 #3B5BDB·'진행률 N%')·강조색·아이콘·보조문구 포함 |
| 헤더 역할 전환 데모 스위처 | P1 | 상단바 '역할 전환 · 데모' + 직원/그룹장/관리자 pill 토글(관리자 활성) | 로고 + 사용자칩 + 로그아웃만, 토글 없음 | 공통 헤더(`AppShell.tsx`)에 추가. 데모 전용 가능성 — 제품 확인 필요 |
| 사이드바 '역할' 섹션 | P1 | 작성/목록/검수/관리 아래 '역할' 구분 헤더 + 직원/그룹장/관리자 링크 그룹 | 4개 항목만, '역할' 섹션 없음 | `AppShell.tsx` nav에 '역할' 그룹 추가. 데모 전용 가능성 확인 필요 |
| 탭+필터+테이블 단일 카드 그룹핑 / 순서 | P1 | 탭(전체/미제출/…) 먼저 → 그 아래 하나의 둥근 흰 보더 카드 안에 필터행+테이블 | 필터 폼이 타이틀 직하(카드 밖) → 탭(하단 보더만) → 테이블만 별도 카드 | `reports/page.tsx` 탭(94-104)·필터(80-92)를 테이블 카드(106) 내부로 이동, 단일 카드로 통합(탭→필터행→테이블) |
| 기간 필터 컨트롤 유형 | P1 | '오늘' 표시 select 드롭다운(▼) | 네이티브 date input(달력 아이콘) `<input type="date">` | `reports/page.tsx:83` date input을 기간 프리셋 select로 교체 |
| 적용 버튼 | P1 | 필터행에 '적용' 버튼 없음(즉시 반영형) | 파란 '적용' 버튼 존재 | `reports/page.tsx:91` 버튼 제거(또는 자동 제출). SSR 폼 동작상 필요할 수 있어 제품 결정 |
| 테이블 '날짜' 컬럼 누락 | P1 | 헤더 직원/날짜/상태/검수/완결율/지연/제출시각 중 '날짜'(06-18) 존재 | 헤더 직원/상태/완결율/지연/제출시각, '날짜' 없음 | `reports/page.tsx:109` 헤더 배열·행에 '날짜' 컬럼 추가 |
| 테이블 '검수' 컬럼 누락 | P1 | 상태와 분리된 '검수' 전용 컬럼 존재 | '검수' 컬럼 없음 | `reports/page.tsx:109` 헤더·행에 '검수' 컬럼 추가. 검수 컬럼 내용 확정 필요 |
| 페이지네이션 푸터 | P1 | 카드 아래 '총 18건'(좌) + 페이지네이션(‹ 1 2 ›, 1 활성 파란 배경)(우) | 총건수·페이지네이션 전무(테이블만) | `reports/page.tsx` 카드(106) 하단에 '총 N건' + 페이지네이션 추가. 현재 전체 행 일괄 렌더라 페이지네이션 로직 필요 |
| 페이지 서브타이틀 문구 | P2 | 설명형 '오늘 팀의 제출·검수 상태를 한눈에 확인하세요.' | 메타형 `{전체 팀/group} · {date}` | `reports/page.tsx:78` 설명형 문구로 교체 |
| 직원 필터 드롭다운 라벨 | P2 | '전체 직원' | '전체 팀' | `reports/page.tsx:86` option 텍스트 변경 |
| 검색 입력 아이콘/플레이스홀더 | P2 | 좌측 돋보기 아이콘 + '이름·업무 내용으로 검색' | 아이콘 없음 + '이름·팀 검색' | `reports/page.tsx:90` search 아이콘 추가, placeholder 변경 |
| 행 액션 버튼 라벨/형태 | P2 | '상세 ›'(쉐브론) 텍스트 버튼 | '보기' 버튼(쉐브론 없음) | `reports/page.tsx:127` 라벨·스타일 변경(검수대기 분기는 별도) |
| 테이블 컬럼 순서 | P2 | 직원/날짜/상태/검수/완결율/지연/제출시각 | 직원/상태/완결율/지연/제출시각(날짜·검수 누락) | 날짜·검수 추가 후 `reports/page.tsx:109` 순서를 디자인과 정확히 일치 |

### 3. list-emp (내 보고서 — 직원)

| 영역 | severity | design | impl | fix |
|---|---|---|---|---|
| 반려 알림 배너 | P1 | 부제 아래/탭 위에 연적색 배경(#FCEBEB)·적색 테두리·라운드 + 좌측 ↩ 아이콘 + '반려된 보고서가 1건 있어요. 사유를 확인하고 재작성해 주세요.' | 직원 뷰에 배너 전무 | `reports/page.tsx:164` 부제 div 다음에 반려 건수>0 시 배너 추가(allMine status==='반려' 카운트, 배경 #FCEBEB / 테두리 #F5C2C2 / ↩ 아이콘) |
| 필터 행(날짜 드롭다운 + 검색) | P1 | 탭과 표 사이에 '오늘' 셀렉트 + 돋보기 아이콘 '업무 내용으로 검색' 검색 input(폭 넓게) | 직원 뷰에 필터 행 전무(매니저 분기에만 존재) | `reports/page.tsx:174-175` 사이에 필터 행 추가(기간 select + flex:1 검색 input, 돋보기 아이콘) |
| 표 컬럼 구성 | P1 | 7컬럼: 직원/날짜/상태/검수/완결율/지연/제출시각 (첫 컬럼 아바타, 상태~완결율 사이 검수) | 5컬럼: 날짜/상태/완결율/지연/제출시각 ('검수' 컬럼 누락) | `reports/page.tsx:177` 헤더·179-187 행에 '검수' 컬럼 추가, 순서 일치. ('직원' 아바타 컬럼은 본인 단일 화면이라 데모 아티팩트로 보류 — 검수 컬럼만 확정) |
| 하단 페이지네이션/총건수 푸터 | P1 | 표 아래 '총 18건'(좌) + 페이지네이션(‹ 1 2 ›, 1 #3B5BDB 파란 채움)(우) | 표 카드(191)로 끝, 총건수·페이지네이션 전무 | `reports/page.tsx:191` 다음에 '총 N건' + 페이지 컨트롤 골격 추가(미구현이면 최소 카운트+골격) |
| 탭 라벨 문구 | P2 | 세 번째 탭 '제출완료' | 세 번째 탭 '제출' | `reports/page.tsx:155` label '제출'→'제출완료' |

### 4. review (검수 상세) — 소스 기준 비교

> 구현 스크린샷은 검수 '대기 목록' 빈 상태('전체 · 검수 대기 0건' / '검수할 보고서가 없습니다.')만 캡처됨. ReviewDetail.tsx 렌더 화면 미캡처라 픽셀 비교 불가. 아래는 디자인 이미지 vs ReviewDetail.tsx(소스) 비교. **검수 대기 1건 이상 시드 후 /review/[id] 재캡처 필요.**

| 영역 | severity | design | impl | fix |
|---|---|---|---|---|
| 스크린샷 불일치(재캡처 필요) | P0 | 검수 상세 화면(헤더카드·버킷·커뮤니케이션·검수이력·AI패널·반려/승인 액션바) | 검수 대기 목록 빈 상태만 캡처 | 검수 대기≥1건 시드 상태에서 `/review/[id]` 재캡처해야 픽셀 정합 검증 가능 |
| 헤더 카드 상태/메타 배지 | P1 | 우측 배지 3개('⌛ 검수대기','↻ 재제출','! 지연') + 그 아래 우측 '완결 7 · 지연 2 · 미완 1 · 전체 10' 집계 라인 | 단일 view.status 배지 하나만(263), 보조 배지·집계 라인 없음 | `ReviewDetail.tsx:263` 인근에 보조 상태 배지 + buckets/sections 집계 요약 라인 추가 |
| 헤더 카드 제출 메타 라인 | P1 | 날짜 아래 '최초제출 11:48 · 반려 13:05 · 재제출 17:54' 타임스탬프 라인 | ownerName/dept(260) + dateLabel(261) 두 줄만, 시각 메타 없음 | `ReviewDetail.tsx:261` 아래에 events에서 submitted/rejected/resubmitted 시각 추려 메타 라인 추가. ReviewView 매핑 필요 |
| 일일 코멘트 배치 순서 | P1 | 일일 코멘트 블록이 헤더 카드 직후·버킷 위 | dailyComment를 본문 맨 아래(커뮤니케이션 다음, 333-338)에 렌더 | `ReviewDetail.tsx:333-338` 블록을 헤더 카드(256-265) 직후, 버킷(285) 앞으로 이동 |
| 업무 버킷 헤더 라벨/시간대/완료 배지 | P1 | '오전 계획·마감 08:30~11:50' / '오후 마감 11:50~17:50' + 우측 초록 '✓ 마감완료' 배지 | name='오전'/'00:00~11:59', name='오후'/'12:00~19:59'(288-289), 우측 회색 '{n}건' 카운트 칩(222) | `ReviewDetail.tsx:288-289` 라벨/range를 디자인 문구로, Bucket 헤더 배지(222)를 구역 상태(마감완료) 색상 배지로 교체 |
| 우측 컬럼 카드 순서(야간/검수이력 역전) | P1 | 야간 카드(최상단) → 검수 이력 → AI 패널 | 검수 이력(345-358) → 야간(360-369) → AI(371-382). 주석(343)도 디자인과 반대 | `ReviewDetail.tsx` 야간 블록(360-369)을 검수 이력(345) 앞으로 이동, 주석(343) 정정 |
| AI 분석 패널 내부 구조 누락 | P1 | 점선 내부 카드 + '✦ 분석 결과 표시 영역' 서브헤더(보라) + 3개 바(—) + 하단 안내문 '효율성·연속성·연관성 분석은 다음 버전에서 제공됩니다. 검수는 AI 없이도 진행할 수 있어요.' | 헤더(✦ AI 분석 결과) + 부제('이번 버전 미개발 · 영역 예약') + 3개 바만(371-382). 점선 래퍼·서브헤더·안내문 없음 | `ReviewDetail.tsx:374-381` 바 영역을 점선 border 내부 카드로 감싸고 서브헤더·하단 안내문 추가 |
| 하단 액션바 좌측 집계 요약 | P1 | 액션바 좌측 '완결 7 · 지연 2 · 미완 1' 요약 (우측 반려/승인 버튼) | openRejectCount>0일 때만 '행 반려 N건'(402), 평소 좌측 비어있음 | `ReviewDetail.tsx:402` 인근에 buckets/sections 기준 완결·지연·미완 집계 요약 추가 |
| 야간 업무 카드 빈 상태 형태 | P2 | 우측 인라인 1줄 '🌙 야간 업무 없음  작성자가 야간 업무를 보고하지 않았습니다.'(회색 보조설명) | '야간 업무' 제목 별도 줄(362) + 본문 '야간 업무 없음'(366)만, 보조설명 없음 | `ReviewDetail.tsx:362-367` 아이콘+'야간 업무 없음'+회색 보조설명을 한 줄 인라인으로 구성 |
| 검수 이력 진행 상태 항목 | P2 | 각 항목 도트+라벨('제출'/'보고서 반려'/'재제출'/'검수 대기 중')+시각+부가설명, 진행 상태 '검수 대기 중' 항목 포함 | EVENT_LABEL 기반(352), 현재 진행 상태('검수 대기 중') 항목 없음, '반려' 문구 디자인('보고서 반려')과 상이 | EVENT_LABEL/events 매핑에 '검수 대기 중' 진행 항목 추가 또는 라벨 조정. 서버 events 매핑 확인 |
| 좌측 사이드바 검수 하위 상태 트리 | P2 | '검수' 하위 '상태(파이프라인)' 그룹 + 검수대기/승인/반려/재제출/종기 들여쓰기 | '작성/목록/검수/관리' 4개 플랫 항목만 | 공통 레이아웃(`AppShell.tsx`)에 검수 하위 상태 네비 추가. ReviewDetail.tsx 소관 아님 |

### 5. admin (사용자 관리) — **감사 불가 (SSoT 매핑 오류)**

| 영역 | severity | design | impl | fix |
|---|---|---|---|---|
| 디자인-구현 화면 자체 불일치 | P0 | design 파일(관리자__관리.png)은 파일명만 '관리'이고 내용은 **검수 상세 화면**(김서연 보고서·검수 배지·일일 코멘트·업무 카드·검수 이력·AI 분석) | impl(방준호__관리.png)은 `admin/users/page.tsx`의 **사용자 관리**(제목 '사용자 관리', '총 4명', '+ 사용자 추가', 이름/아이디/소속/역할/상태 테이블) | 두 화면이 완전히 다름 → 픽셀/구조 정합 비교 불가. admin(사용자 관리)에 대응하는 **올바른 디자인 PNG로 SSoT 교체** 후 재감사. 디자인 에셋 경로/매핑 점검 필요 |

---

### 화면별 '차이 0' 여부
- **write**: 차이 0 아님 — 확정 차이 11건 (P1 7 / P2 4).
- **list-mgr**: 차이 0 아님 — 확정 차이 13건 (P0 1 / P1 7 / P2 5).
- **list-emp**: 차이 0 아님 — 확정 차이 5건 (P1 4 / P2 1).
- **review**: 차이 0 아님 — 확정 차이 11건 (P0 1[재캡처] / P1 7 / P2 3). 단 픽셀 정합은 상세 화면 재캡처 후 재검증 필요.
- **admin**: 판정 불가 — 디자인 SSoT가 검수 화면으로 잘못 매핑되어 감사 불가(P0 1). 올바른 PNG 제공 후 재감사 필요.